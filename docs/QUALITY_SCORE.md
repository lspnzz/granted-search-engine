# Quality Score

| Area | Current Grade | Target | Notes |
|---|---:|---:|---|
| Repo knowledge | C | A | Docs are now structured, but need ongoing updates as behavior changes. |
| Unit tests | D | B | Add focused coverage around service boundaries and graph flow. |
| Evals | C | A | Executable smoke eval exists; live evals can be scheduled later. |
| CI gates | C | A | PR checks should run before deploy workflows. |
| Local observability | C | B | Structured logs and request IDs are v1; full local traces can follow. |
| Frontend validation | C | B | Playwright covers core journeys once browsers are installed. |

Update this file when a quality gap is closed or a new recurring failure pattern appears.
